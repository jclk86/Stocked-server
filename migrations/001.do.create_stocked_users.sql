CREATE TABLE stocked_users (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  username TEXT NOT NULL UNIQUE,
  fullname TEXT NOT NULL,
  password TEXT NOT NULL,
  email TEXT NOT NULL,
  date_created TIMESTAMP DEFAULT now() NOT NULL
);